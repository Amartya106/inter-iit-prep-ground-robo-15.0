# Part 2 - Manipulator RL. Python 3.10 (pybullet wheel availability).
#
#   uv venv --python 3.10 .venv
#   uv pip install -r requirements.txt
#
# Torch is pinned to the CPU build here, for a submission meant to run on
# any machine. The project itself used both: phases 1-4's main checkpoints
# trained on CPU, the later Phase 4 insertion investigation (R1-R6) trained
# on GPU (a separate .venv_gpu_test with CUDA torch). Benchmarked directly
# (runs/bench_cpu_clean.log vs runs/bench_gpu_clean.log): about 1450-1490
# steps/s on CPU vs 1500-1550 on GPU. PyBullet physics stepping is the real
# throughput bottleneck, not the ~100k-parameter policy/critic MLPs, so
# which one you use barely changes the number.

pybullet==3.2.6
gymnasium==0.29.1
numpy==1.26.4

# RL
torch==2.2.2
stable-baselines3==2.3.2
sb3-contrib==2.3.0          # TQC

# logging / eval / plots
tensorboard==2.16.2
pandas==2.2.2
matplotlib==3.8.4
imageio==2.34.1
imageio-ffmpeg==0.4.9
pyyaml==6.0.1
tqdm==4.66.4
rich==13.7.1
